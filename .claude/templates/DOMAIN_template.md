# DOMAIN_<NAME> — <короткое описание в одну строку>

Шаблон нового доменного doc'а. Заменить плейсхолдеры, удалить эту строку.
Документируй то, что НЕ видно из кода: инварианты, бизнес-правила, грабли. Не дублируй сам код, списки эндпоинтов и историю изменений.

## Таблицы
| Модель | Назначение | Ключ / constraint |
|--------|------------|-------------------|
| `Foo` | ... | `(project_id, code)` |

## Бизнес-правила
Инварианты, которые нельзя нарушать при изменениях. Это главная ценность дока — то, что агент не выведет из кода.
- Правило 1
- Правило 2

## Зависимости
- `DOMAIN_X` — для ...
- WB API — для ...

## Грабли
Нюансы, чтобы не наступить второй раз. Описывай саму проблему и как её избежать — без хешей коммитов и дат (их найдёт `git blame`).
- **Грабля A** — в чём суть и как не повторить.

## Файлы
- `backend/services/<domain>_service.py` — основная логика.
- `backend/routers/<domain>.py` — HTTP endpoints.
