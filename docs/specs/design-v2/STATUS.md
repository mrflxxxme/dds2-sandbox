# STATUS — правки модуля «Дизайн карточек» (v2)

<!-- HEAD-SUMMARY: rolling-состояние волн A–D. Обновляет lead на шаге Close каждой волны (цикл — CHARTER v1 §4). Формат строки — CHARTER v1 §11. -->

**Обновлено:** 2026-08-20 · **Стадия:** волны A–D написаны, проверены и прошли сводное ревью шести агентов; правки по ревью внесены и перепроверены. Ждут подписи архитектора на волны B, C, D и мержа.
База — `sandbox/main`, коммит `31dd0618` (голова песочницы на момент написания; перед стартом каждой
волны — `git fetch sandbox && git log -1 sandbox/main`, ветка берётся от актуальной головы).

Спецификация прошла два независимых аудита: на внутреннюю связность (28 находок, все закрыты)
и на соответствие фактическому коду (9 находок, все закрыты). Существенное из второго:
компонент `InfoTip` в репозитории уже есть и поднимается, а не пишется заново; `AdsPeriodPicker`
имеет четырёх потребителей, не одного; revision id головы миграций — `dsnmrg_design_dev`
(не имя файла); порядок роутов существующим тестом не покрывается — каждая волна пишет свой.

## Прогресс

| Волна | Название | Статус | Дата | Commit | Evidence | Подпись архитектора |
|---|---|---|---|---|---|---|
| A | UI-правки + двухмесячный календарь | ✅ signed | 2026-08-20 | `30be4804` | ниже | **подписано владельцем 2026-08-20** (`schemas/design.py`, `globals.css` — см. DECISIONS-LOG 2026-08-20) |
| B | Редактируемый номер заявки | 🟢 done-unsigned | 2026-08-20 | `a5503c8d` | 20 тестов, миграция up→down→up | **требуется ДО мержа** (models/migrations/schemas) |
| C | Метки + справочник реквизитов | 🟢 done-unsigned | 2026-08-20 | `76a393e1` | 38 тестов, миграция up→down→up | **требуется ДО мержа** (models/migrations/schemas/globals.css) |
| D | Вкладка «Аналитика» + XLSX | 🟢 done-unsigned | 2026-08-20 | `2eb3dce1` | 28 тестов, миграция up→down→up | **требуется ДО мержа** (models/migrations/schemas) |
| — | Правки по сводному ревью A–D | 🟢 done-unsigned | 2026-08-20 | — | таблица «Сводное ревью» ниже | не нужна отдельно: tripwire-файлы (`schemas/design.py`) правились в рамках уже подписанных волн |

Статусы: ⏳ planned · 🔨 active · 🟢 done-unsigned · ✅ signed.

## Блокеры / действия архитектора

| Приоритет | Что | От кого | Статус |
|---|---|---|---|
| — | Подпись волны A: `schemas/design.py` (+`truncated`) и `globals.css` (+2 класса подсказки) | архитектор | ✅ подписано 2026-08-20 |
| — | продуктовых развилок нет: Р17–Р33 ратифицированы интервью 2026-08-19 (4 раунда) | — | — |

## Открытые вопросы

Нет. Все 14 развилок, включая четыре ранее адресованные заказчику, закрыты владельцем
2026-08-19 — см. DECISIONS-LOG записи №1–№3.

Решения, принятые за заказчика (если Денис возразит — правка дешёвая, ни одно не в фундаменте):

| Что решили | Как | Цена смены |
|---|---|---|
| «Круги цвета» = несколько меток + счётчик истории | Р20, история через `removed_at` | нулевая: данные пишутся, меняется только UI |
| Тултипы по всему модулю, а не точечно | Р24 | нулевая: убрать лишние вызовы `InfoTip` |
| Сид брендов — со скриншота заказчика | Р33 | нулевая: правится в «Настройках» |
| Динамика по неделям не входит в v1 | §5 | добавление виджета в дашборд |

## Заметки исполнения

- Контур Р16 v1 действует: пуш только в `sandbox`, в `origin` не пушится ничего, деплой не выполняется.
- Каждая волна = отдельная ветка от свежего `sandbox/main` → PR в `sandbox/main` (Р25); мерж по порядку A→B→C→D.
- Цепочка миграций v2: `dsn04` (B) → `dsn05` (C) → `dsn06` (D). Перед каждой — `alembic heads` в контейнере, `down_revision` цепляется за фактическую голову, не за локальный хвост.
- Запуск песочницы — шапка `docker-compose.sandbox.yml`; после правок кода пересобирать `--build` (код запекается в образ).
- Скип `test_frontend_page_keys_are_known_to_backend` в контейнере — штатный (нет `frontend-react` в образе backend).

## Сводное ревью волн A–D — что нашли и что сделали

Шесть профильных агентов по diff-путям (code · security · database · performance · api · frontend).
Ниже — только находки, по которым что-то изменилось; полный разбор с обоснованиями — в
DECISIONS-LOG (запись 2026-08-20T06:00).

| # | Находка | Что сделано | Файл |
|---|---|---|---|
| HIGH-4 | Массовая разметка (WC §7, AC-10) не реализована во фронте: `bulkDesignLabels` / `bulkDesignAttributes` — мёртвые методы | Чекбоксы строк + панель «Выбрано N» с «Проставить»/«Снять» | `components/BulkMarkupBar.tsx` (новый), `components/ListView.tsx` |
| M8 | «Настройки» не умеют переименовывать и менять порядок: `updateDesignLabel` / `updateDesignAttribute` / `updateDesignAttributeValue` не звались ниоткуда | Инлайн-правка метки (имя + цвет), поля (имя + `is_multi`), значения; drag-порядок меток | `settings/page.tsx` |
| M-4 · F2 · M6 | Массовая операция делала 3 запроса и **коммит на каждую** из 500 задач | Задачи и текущая разметка — по одному префетч-запросу на батч, коммит один | `services/design/refs.py` |
| M3 | «SELECT-проверка → INSERT» в справочнике не атомарна: гонка давала 500 вместо контрактного 400 | Advisory-lock `_REFS_LOCK_CLASS` на create_* И update_* | `services/design/refs.py` |
| API #6 | `usage_count` считал связи мягко удалённых задач и расходился с `/stats/by-attribute` | JOIN с `design_tasks` + `is_deleted = false` во всех трёх счётчиках | `services/design/refs.py` |
| F7 | Деталка и фильтры доски тянули `usage_count`, который не рисуют | Параметр `with_usage`; счётчик считают только «Настройки» | `refs.py`, `routers/design_tasks.py`, `lib/api/designTasks.ts` |
| S-4 | `VIEWER_WRITABLE_ROUTES` ключевался путём: исключение для PUT молча накрыло бы будущие DELETE/POST | Ключ — пара «МЕТОД путь» | `tests/test_rbac_page_gates.py` |
| S-5 | Архив справочника растёт без потолка, выборка с ним усекалась молча и могла вытеснить активные записи | `ORDER BY is_archived` первым ключом + `logger.warning` на усечении | `services/design/refs.py` |
| SEC | В XLSX уходили сырые пользовательские строки: `=` в начале — живая формула Excel | `_txt()` на всех пользовательских ячейках, включая заголовки колонок реквизитов | `services/design/export_xlsx.py` |
| PERF | Сборка книги на 5000×60 держала event loop | `asyncio.to_thread(_render)` после последнего чтения БД | `services/design/export_xlsx.py` |

**Дефекты, вскрытые собственным прогоном (не ревьюерами):**

- `_window` → `stats_window` был переименован `replace_all` и задел `resolve_stats_window`, породив
  `resolve_statsstats_window`. Аналитика падала `NameError` на всех четырёх ручках. Исправлено;
  27 красных тестов стали зелёными.
- Ограничение `task_ids` в схеме подменяло контрактный 400 «Не больше 500 задач за раз» на 422 —
  снято, потолок держит сервис (см. DECISIONS-LOG).
- `test_layout_validation` требовал 422, а контракт обещает 400 — тест приведён к контракту.
- `test_ac5_stats_exact_values_on_six_tasks` опирался на «за всё время» у `GET /stats` без дат —
  переведён на явное окно, новый дефолт закреплён отдельным ассертом.

### Гейты после правок

| Гейт | Результат |
|---|---|
| `pytest -k design` | **298 passed**, 0 failed |
| `pytest test_rbac_page_gates.py test_conventions_sync.py` | 82 passed, 3 skipped |
| `mypy` (18 файлов модуля) | Success: no issues found |
| `tsc --noEmit` | чисто |
| `vitest run` | **111 файлов, 1547 тестов, 0 падений** |
| `check_conventions.sh` | PASSED, 9 предупреждений (все — доCommit'ные, в чужих файлах) |
| Миграции | `alembic heads` = одна голова `dsn07`; up → down → up чисто. Дополнительно — прогон **с нуля на чистой БД** (`dds_migtest`): вся цепочка `base → dsn07`, затем `dsn07 → dsn04 → dsn07`. Он был нужен: dsn05 и dsn06 правились ПОСЛЕ того, как их уже применили к песочнице, поэтому в её БД новых FK-индексов не было, а `downgrade` dsn06 падал на `DROP INDEX` несуществующего индекса. На чистой БД все четыре FK-индекса создаются, и обе стороны симметричны. Урок: правку уже применённой миграции нельзя проверять на той же БД. |

### Браузерная проверка правок (песочница `dds2-sandbox`, свежая сборка)

| Что проверено | Результат |
|---|---|
| Чекбоксы строк и «Выделить все видимые» | рисуются, `aria-label` на каждом |
| Панель «Выбрано 2» | селект метки, плоский селект «Поле — Значение», кнопки «Проставить»/«Снять»/«Сбросить выбор» |
| Массовое проставление метки + реквизита двум задачам | 2 запроса (`bulk/labels`, `bulk/attributes`), оба 200; в БД у DES-1 и DES-2 появились «Срочно» и «Ozon»; выбор сброшен |
| Переименование метки | «Срочно» → «Горит» с цветом `violet` — в БД имя и цвет обновились |
| Drag-порядок меток | «Топ-товар» перетащен в начало → `sort_order` 0/10/20; PUT ушёл ровно на **две** сдвинувшиеся метки, третью не трогали |
| `with_usage=false` | доска и деталка задачи шлют его; «Настройки» — нет (им счётчик нужен) |
| `usage_count` и мягкое удаление | до удаления DES-2 «Горит» = 2, после `DELETE /design-tasks/3509` = 1 |

## Волна A — evidence

Прогоны в изолированном контуре `dds2-sandbox` (собран из этой ветки, БД своя):

| Гейт | Результат |
|---|---|
| `pytest tests/test_design_v2_calendar_range.py` | **7 passed** |
| `pytest tests/ -k design` (регресс модуля) | **209 passed**, 5873 deselected |
| `mypy` по дизайн-скоупу | Success, no issues in 15 source files |
| `bash scripts/check_conventions.sh` | PASSED, 9 предупреждений (все довольные волны — прежние) |
| `npx tsc --noEmit` | чисто |
| `npx vitest run src/__tests__/lib/` | **91 файл, 1369 тестов, все зелёные** |
| Целевые юниты волны A | `boardFilters` 8 · `periodPickerPresets` 5 · `designCalendar` 33 (+6 новых) · `design` 10 |

Браузерные проверки (127.0.0.1:13000, сид: проект «Волна A», 4 задачи):

| AC | Что подтверждено |
|---|---|
| AC-3 | Вкладок четыре — Доска · Список · Календарь · Загрузка; «Все бренды» нет |
| AC-4 | Смена фильтра и ввод поиска — **0** запросов; первое переключение на «Отложенные» — **ровно 1** (`GET /design-tasks?status=ON_HOLD&limit=200`); ещё четыре переключения туда-обратно — **0** |
| AC-5 | Дефолт — два блока (Август + Сентябрь 2026); стрелка «вперёд» → 01.09–31.10, длина сохранена; «Сегодня» возвращает дефолт; переключатель раскраски меняет цвет чипа (серый → синий) за **0** запросов |
| AC-6 | «30 дней» даёт `date_from=2026-07-22&date_to=2026-08-20` на воронке, в списке кампаний и в лестнице СПП — окно включительное, как до рефакторинга; `grep AdsPeriodPicker` пуст |
| AC-7 | Контраст подсказки **9.37:1**; `aria-describedby` связывает элемент со всплывашкой; открывается по тапу и закрывается повторным; из вьюпорта не выходит |
| AC-8 | 🖌️ в меню и заголовках; `grep 🎨` по `p/[slug]` пуст |
| §6 | Подсказки: 6/6 колонок доски · 6/6 метрик · чипы списка · красная точка · бейдж «Срочно» · переключатель раскраски · кнопки деталки |

## Волна A — ревью

`/review` четырьмя профильными агентами (code · frontend · api · performance), Opus, параллельно.

| Агент | Вердикт | Находки |
|---|---|---|
| code-reviewer | WARNING | 0 CRITICAL · 1 HIGH · 4 MEDIUM · 6 LOW |
| frontend-reviewer | WARNING | 0 CRITICAL · 1 HIGH · 6 MEDIUM |
| api-designer | WARNING | 0 breaking · 3 по тестам · 2 INFO |
| performance-optimizer | без блокеров | 2 MEDIUM с реальным влиянием + 3 LOW |

**Сводный вердикт: WARNING, BLOCK нет.** Обе HIGH и все MEDIUM, относящиеся к волне, исправлены —
перечень в DECISIONS-LOG 2026-08-20T01:00. Существенное из пойманного: NUL-байт в литерале
(файл был бинарным для git), тихое усечение набора вне доски на 200 строк, обрезка попапа пикера
внутри модалки, `OverflowError` вместо 400 на краю календаря, три теста, закреплявших не то,
что обещали их докстринги.

## Волна B — evidence

| Гейт | Результат |
|---|---|
| `alembic upgrade head → downgrade -1 → upgrade head` | чисто, голова одна: `dsn04_design_number_len` |
| `pytest tests/test_design_v2_number.py` | **20 passed** |
| `pytest tests/test_design_v2_calendar_range.py` (после правок ревью) | **10 passed** |
| `pytest tests/ -k design` | **232 passed** (было 209 до волны B) |
| `mypy` по дизайн-скоупу · `tsc` · `vitest src/__tests__/lib/` | чисто · чисто · 1372 зелёных |

## Волна C — evidence

| Гейт | Результат |
|---|---|
| `alembic upgrade head → downgrade -1 → upgrade head` | чисто, голова одна: `dsn05_design_labels_attributes` |
| `pytest test_design_v2_refs.py + test_design_v2_task_labels.py` | **38 passed** |
| `pytest tests/ -k design` | **270 passed** (было 232 после волны B) |
| `mypy` по дизайн-скоупу | Success, no issues in 16 source files |
| `check_conventions.sh` | PASSED, те же 9 предупреждений |
| `tsc --noEmit` · `vitest src/__tests__/lib/` | чисто · **1378 зелёных** |

Контраст палитры меток к фону карточки (`#fbfbfc` = `--color-bg-card` поверх `--color-bg`):
red 4.67 · orange 3.83 · amber 5.25 · green 5.50 · teal 5.39 · blue 5.79 · violet 6.40 ·
pink 5.60 · brown 5.68 · slate 7.43 — все ≥ 3:1 с запасом.

Два существующих гварда сработали как задумано и потребовали правки:
`test_all_mutations_have_rate_limit_write` читает текст ДЕКОРАТОРА, поэтому
`rate_limit_write` у новых ручек переехал из параметра в `dependencies=[...]`;
`test_board_returns_permissions_by_role` сверяет полный словарь прав доски и
поймал новый флаг `can_manage_refs`. Оба обновлены осознанно, не «под зелёное».

## Волна D — evidence

| Гейт | Результат |
|---|---|
| `alembic upgrade head → downgrade -1 → upgrade head` | чисто, голова одна: `dsn06_design_dashboard_layout` |
| `pytest test_design_v2_stats.py + test_design_v2_export.py` | **28 passed** |
| `pytest tests/ -k design` | **298 passed** (было 270 после волны C) |
| `pytest tests/test_rbac_page_gates.py` | **78 passed** |
| `mypy` по дизайн-скоупу | Success, no issues in 18 source files |
| `check_conventions.sh` · `tsc` · `vitest src/__tests__/lib/` | PASSED (те же 9) · чисто · 1378 зелёных |

XLSX проверяется не «ручка ответила 200», а чтением файла обратно через openpyxl
в самом тесте: сверяются состав листов, цифры «Сводки» с ответами ручек, формат
мультизначных ячеек и обе строки усечения.

**Гвард RBAC потребовал точечного исключения.** `tests/test_rbac_page_gates.py`
требует, чтобы ни одна мутация домена не была доступна viewer'у, — а `PUT
/dashboard/layout` по Р32 именно такая. Вместо ослабления правила заведён
отдельный список `VIEWER_WRITABLE_ROUTES` с обоснованием: ручка не пишет данных
проекта, `user_id` берётся из сессии, page-гейт и `rate_limit_write` на месте.
Общий гвард продолжает действовать на все остальные ручки модуля.

## Браузерные проверки волн C и D

Песочница `127.0.0.1:13000`, проект «Волна CD»: 3 метки, поле «Бренд» с тремя
значениями, 4 задачи с разметкой.

| Что | Результат |
|---|---|
| Вкладок шесть | Доска · Список · Календарь · Загрузка · Аналитика · Настройки |
| Палитра доехала до браузера | кружок метки красится `rgb(217, 45, 32)` = `--color-label-red`; чипы в «Настройках» — red/amber/green ровно по токенам |
| Фильтр доски по метке | сузил доску до 2 карточек за **0** сетевых запросов (18 → 18) |
| Селекты фильтров | «Все исполнители», «Все типы работ», «Все метки», «Все: Бренд» |
| Дашборд | 4 виджета, по одному запросу на виджет, у каждого своё empty-состояние |
| Скрытие виджета | «Воронка» исчезает из карточек, раскладка уходит `PUT /dashboard/layout`, повторных запросов виджета нет; возврат восстанавливает |
| «Настройки» | usage_count («в 2 задачах»), выбор из 10 цветов, живой предпросмотр чипа |

## evidence_gap

- **Карточка кампании (`ads-manager/campaign/[campanId]`) вживую не проверена** — в чистой песочнице нет ни одной кампании WB. Три других потребителя `PeriodPicker` проверены браузерно, четвёртый закрыт только `tsc` и одинаковым кодом вызова.
- **Правая граница вьюпорта у подсказки** проверена на элементе, который до края не доходит: сработало общее ограничение `Math.min/max` в `Tooltip` (код не менялся и уже работает в рекламе), но кейса «подсказка ровно у края» в прогоне не было.
- ~~**12 файлов фронт-тестов падают в локальном окружении** (`React.act is not a function`)~~ — **СНЯТО 2026-08-20.** Причина оказалась не в коде и не в окружении как таковом, а в битом `node_modules`: установка была сделана без `--legacy-peer-deps` (его требует `@tremor/react` на React 19 — см. `.github/workflows/test.yml`) и осталась частичной. После чистого `npm ci --legacy-peer-deps` весь фронт зелёный: **111 файлов, 1547 тестов, 0 падений**, включая `globalsBackdropFilter`. Вывод на будущее: «падает и на нетронутом HEAD» доказывает, что дефект не в правке, но НЕ доказывает, что он вообще в коде.
- **Три сервиса модуля переросли порог 500 строк** и висят в предупреждениях `check_conventions.sh`: `refs.py` (932 — справочники + разметка задачи + массовые операции в одном файле), `queries.py` (671, было 474 до волны A), `files.py` (627, порог перешагнул ещё до v2). `analytics.py` (500) стоит ровно на границе. Распил — отдельная задача: он затрагивает `to_list_items`, у которого три потребителя, и смешивать его с фичей нельзя. Естественная линия разреза `refs.py` — «CRUD справочника» / «разметка задачи и батчи».
