# Итоги работы — модуль «Дизайн карточек»

**Дата:** 3 августа 2026 · **Ветка:** `claude/dds2-card-design-spec-8a2392` в `mrflxxxme/dds2-sandbox` · **Статус:** готово, синхронизировано с `dev`, ждёт вашей приёмки

> Карта всех веток и порядок переноса в основной репозиторий — `docs/BRANCHES-HANDOFF.md`.

---

## Что сделано

Модуль «Дизайн карточек» построен целиком — от спецификации до работающей сборки, которую можно поднять локально одной командой. Реализовано всё, что обещал PRD v4.0: канбан-доска с перетаскиванием, восемь состояний задачи со строгими переходами и неудаляемым журналом, версии сдач с вердиктами, календарь на месяц, экран загрузки команды, сквозной список по брендам, личные Telegram-уведомления и утренняя сводка, мост в А/Б-тесты фото, панель метрик.

| Показатель | Значение |
|---|---|
| Коммитов | 16 (включая слияние с `dev`) |
| Файлов затронуто | 121 |
| Строк добавлено | 16 537 |
| Тестов модуля | 193 бэкенд + 13 фронт-юнитов |
| Полная сюита репозитория | 5949 пройдено, 0 упало |
| Фаз исполнено | 8 (Ф0–Ф7) |
| Циклов ревью | 7, из них 1 BLOCK и 6 WARNING — все закрыты |

## Как это делалось

Работа шла по методологии, снятой с ваших проектов WIZOR и ORIION и адаптированной под масштаб модуля: единый keystone-чартер вместо разрозненных документов, фазовые спецификации с машинно-проверяемыми критериями приёмки, три «растяжки автономии» (что агент решает сам, что эскалирует, что не мёржится без человека) и запрет на «зелёное по утверждению» — каждый гейт подтверждался транскриптом прогона.

Каждая фаза проходила цикл: реализация → гейты в песочнице → ревью профильными агентами → фикс-цикл → повторные гейты → коммит. Все решения, принятые агентами самостоятельно, записаны в журнал `DECISIONS-LOG.md` — это ваш пост-фактум аудит-трейл.

## Что нашло ревью

Показательные дефекты, пойманные до того, как стали проблемой:

- **Сводки уходили бы по удалённым проектам** — чтение проектов без фильтра удалённых (нарушение железного правила проекта).
- **Транзакция удерживалась через загрузку в хранилище** — ровно те грабли, что однажды положили пул соединений; переписано на короткие транзакции.
- **Приём не той версии** — при двух несданных версиях вердикт применялся к последней, а не к указанной.
- **Мёртвый код** — вложения к комментариям были реализованы в сервисе и модели, но недостижимы через API.
- **Гонка запросов календаря** — при быстром переключении месяцев показывалось ложное «задач нет»; воспроизведена принудительно и закрыта.
- **Асимметрия в статусной модели** — из «Отложенных» нельзя было вернуться в «Правки», хотя механика это обещала.

## Побочные находки в основном репозитории

Работа над модулем вскрыла три системных дефекта за его пределами. Все исправления лежат в отдельных ветках и **не отправлены** в основной репозиторий — решение за вами.

**1. Загрузка активного содержимого** (`fix/upload-svg-mime`, коммит `64fdace8`). Четыре ручки принимали `image/svg+xml`, доверяя заголовку клиента, — возможен stored XSS. Аудит нашёл вдвое больше уязвимых мест, чем предполагалось. Закрыто общим хелпером, 17 новых тестов.

**2. Права доступа на бэкенде** (`fix/rbac-funnel-fbs-gates`, коммит `26499eae`; отдельно `fix/ab-tests-rbac-gate`, `84fd5c67`). Из 903 ручек API проверку доступа к разделу имели только 38 — четыре процента. Дыра, найденная в А/Б-тестах, где участник с правом просмотра мог подменить живое фото карточки, оказалась не исключением, а нормой: единственной защитой был экран на фронте. Закрыты 11 доменов механизмом fail-closed, включая два самых дорогих — пополнение рекламного бюджета Wildberries и трансляцию остатков с созданием поставок. Написан параметризованный сторожевой тест, который не даст новому роутеру проскользнуть мимо классификации. Попутно закрыты 29 мутаций без ограничителя частоты.

**3. Несуществующие CSS-переменные** (`claude/suspicious-borg-691dc7`, коммит `1dc3dc2e`). Суммарно около 390 вхождений по всему фронту: приглушённый текст рендерился основным цветом. Исправлено вместе с первопричиной — правило проекта описывало палитру неверно и провоцировало ошибку в каждом новом компоненте.

## Что осталось за скоупом

Осознанные границы, зафиксированные в PRD и чартере: публикация комплекта макетов в витрину маркетплейса, портал для подрядчиков, автоподсказки «какие карточки переделать», учёт денег за дизайн, оценка в часах.

Технический долг модуля, не влияющий на работу: два файла превысили рекомендованный порог в 500 строк (роутер и файловый сервис) — кандидаты на распил; таблица задач дублируется между списком и сквозным экраном; в режиме списка идёт тихий фоновый запрос доски ради флага прав на кнопку создания.

## Синхронизация с актуальной базой

Ветка была отпочкована от `main`, а `dev` с тех пор ушёл далеко вперёд. Чтобы модуль не оказался построенным на устаревшем фундаменте, `origin/dev` влит мержем (`582a8f92`): 163 коммита, 225 файлов — новые домены зарплаты, отзывов, трансферов и 25 миграций.

Разрешено при слиянии: в `backend/rbac.py` взята версия `dev` с механикой наследования доступов, ключ `design-tasks` добавлен в четыре структуры (в старой версии их было две); две головы миграций сведены merge-миграцией, цепочка проверена накатом на пустую базу.

Слияние вскрыло дефект, который иначе всплыл бы уже в проде: сквозной экран вызывал функцию прав в старой двухаргументной форме, а в новой сигнатуре это молча выключает наследование — бренд выпадал из списка у всех, кто получил доступ к разделу по наследству, без единой ошибки. Исправлено, полная сюита после слияния — 5949 пройдено, ноль падений.

Ещё одно, для практики: полный прогон тестов стирает данные песочницы — тестовая инфраструктура чистит всё, кроме зарезервированных проектов. Демонстрационные данные заводите после прогона, а не до.

## Как посмотреть

Инструкция — `docs/specs/design/LOCAL-SANDBOX.md`, проверена проходом «глазами новичка». Три команды: клонировать ветку, скопировать шаблон окружения в `.env`, поднять стек. Интерфейс на `http://127.0.0.1:13000`.

Документация модуля: `docs/DESIGN-TASKS-GUIDE.md` — как всё работает для тех, кто пользуется; `backend/DOMAIN_DESIGN.md` — техническая карта для тех, кто сопровождает.
