// Легаси-точка входа (funnel → StockAnalytics, subView='settings').
// Реализация одна — каноническая в warehouse/analytics; локальная копия
// дрейфовала (без preorder/совместимости категорий/🔥) и показывала
// урезанный экран настроек.
export { WarehouseExclusionSettings } from '../../warehouse/analytics/components/WarehouseExclusionSettings';
