/**
 * Единые параметры расчёта потребности сборки (черновик, матрица, потребность
 * по складам, pre-dist машины). Меняются здесь — подхватываются всеми экранами.
 */

/** Горизонт покрытия, дней: «на сколько дней запаса везём» (supply_days). */
export const NEED_SUPPLY_DAYS = 21;

/** Окно анализа скорости продаж, дней (analysis_days) — база growth-aware
 *  скорости (max из среднего за 14д/7д/3д на бэке) и окно ₽/локализации. */
export const NEED_ANALYSIS_DAYS = 14;
